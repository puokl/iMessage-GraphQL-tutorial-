const resolvers = {
  // it can be a query, mutation or subscription resolver
  Query: {
    searchUsers: () => {},
  },
  Mutation: {
    createUsername: () => {},
  },
};

export default resolvers;
